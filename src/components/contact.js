import React from 'react';

const Contact = (props) => {
	// setTimeout(() => {
    //     props.history.push('/about');
    // },8000);
    return (
        <div className="container">
            <h1 className="center">This is the Contact</h1>

            <p>lorem</p>
        </div>
    );
}

export default Contact